route:
  group_by: ["alertname", "job"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: "webhook"

receivers:
  - name: "webhook"
    webhook_configs:
      - url: "${ALERT_WEBHOOK_URL:-http://webhook:5000/}"
        send_resolved: true
